CREATE TABLE zemp_project_manager(emp_id BIGINT REFERENCES employee(emp_id), title_id VARCHAR(15) REFERENCES title(title_id), dept_id VARCHAR(15) REFERENCES department(dept_id), project_id VARCHAR(10) REFERENCES project(project_id),branch_id VARCHAR(10) REFERENCES branch_location(branch_id), team_id VARCHAR(10) REFERENCES team(team_id),role_id VARCHAR(10) REFERENCES role(role_id));
